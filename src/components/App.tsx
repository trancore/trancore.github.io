import React, { FC } from "react";
import "~/components/App.modules.scss";

export const App: FC = () => {
  return (
    <div className="App">
      <header className="App-header">
        <p>🚧工事中。。。</p>
        <p>只今作成中です。</p>
      </header>
    </div>
  );
};
